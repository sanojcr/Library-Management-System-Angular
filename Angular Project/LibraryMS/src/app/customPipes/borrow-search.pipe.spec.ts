import { BorrowSearchPipe } from './borrow-search.pipe';

describe('BorrowSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new BorrowSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
